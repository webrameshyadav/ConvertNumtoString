import { TextBoxcustomDirective } from './text-boxcustom.directive';

describe('TextBoxcustomDirective', () => {
  it('should create an instance', () => {
    const directive = new TextBoxcustomDirective();
    expect(directive).toBeTruthy();
  });
});
